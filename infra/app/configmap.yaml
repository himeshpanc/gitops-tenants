# Kargo's yaml-update writes the tofu output (the greeting) into data.message.
apiVersion: v1
kind: ConfigMap
metadata:
  name: podinfo-greeting
  namespace: infra-demo
data:
  message: greetings from tofu-provisioned OpenBao (module 6.22.0)
